<%= form_for [ user, user.items.new ], remote: true do |f| %>
  <div class='form-group'>
    <%= f.label :Add %>
    <%= f.text_field :name, :style => "width:25%;", class: 'field', placeholder: 'Add a new item.' %>
    <%= f.submit 'Save', class: 'btn btn-success' %>
  </div>
<% end %>
